diff --git a/src/features/gestion-usuarios/api/usuarioZonasApi.test.ts b/src/features/gestion-usuarios/api/usuarioZonasApi.test.ts
index f8d7f9c..2af5cb3 100644
--- a/src/features/gestion-usuarios/api/usuarioZonasApi.test.ts
+++ b/src/features/gestion-usuarios/api/usuarioZonasApi.test.ts
@@ -30,6 +30,7 @@ const createJsonResponse = (
 const createZona = (
   overrides: Partial<UsuarioZonaItem> = {}
 ): UsuarioZonaItem => ({
+  idAsignacion: null,
   idUsuario: 16068,
   idCliente: 59,
   zona: '550',
@@ -85,6 +86,7 @@ export const suite = defineSuite(
                 descripcionZona:
                   '550 Lima                ',
                 bEstado: null,
+                nid_asignacion: null,
               },
             ])
           );
@@ -159,6 +161,10 @@ export const suite = defineSuite(
               16068
             );
 
+          assert.equal(
+            zonas[0]?.idAsignacion,
+            901
+          );
           assert.equal(
             zonas[0]?.nombre,
             '550 Lima'
@@ -231,7 +237,7 @@ export const suite = defineSuite(
     ),
 
     test(
-      'reactiva mediante PUT usando zona como nid_asignacion',
+      'reactiva mediante PUT usando el nid_asignacion real devuelto por la API',
       async () => {
         const originalFetch =
           globalThis.fetch;
@@ -257,6 +263,9 @@ export const suite = defineSuite(
         try {
           await updateUsuarioZona(
             createZona({
+              idAsignacion: 82450,
+              zona: '100',
+              nombre: '100 Ancash',
               estado: false,
             }),
             true
@@ -268,11 +277,11 @@ export const suite = defineSuite(
           );
           assert.equal(
             capturedBody.nid_asignacion,
-            550
+            82450
           );
           assert.equal(
             capturedBody.zona,
-            '550'
+            '100'
           );
           assert.equal(
             capturedBody.bestado,
@@ -286,7 +295,7 @@ export const suite = defineSuite(
     ),
 
     test(
-      'desactiva mediante PUT usando zona como nid_asignacion',
+      'desactiva mediante PUT usando el nid_asignacion real de la zona asignada',
       async () => {
         const originalFetch =
           globalThis.fetch;
@@ -309,6 +318,7 @@ export const suite = defineSuite(
         try {
           await updateUsuarioZona(
             createZona({
+              idAsignacion: 901,
               estado: true,
             }),
             false
@@ -316,7 +326,7 @@ export const suite = defineSuite(
 
           assert.equal(
             capturedBody.nid_asignacion,
-            550
+            901
           );
           assert.equal(
             capturedBody.zona,
@@ -334,53 +344,39 @@ export const suite = defineSuite(
     ),
 
     test(
-      'usa nid_asignacion=1 al reactivar la zona 1',
+      'no ejecuta PUT si una relación existente no trae nid_asignacion',
       async () => {
         const originalFetch =
           globalThis.fetch;
-        let capturedBody:
-          Record<string, unknown> = {};
-
-        globalThis.fetch = async (
-          _input,
-          init
-        ) => {
-          capturedBody = JSON.parse(
-            String(init?.body)
-          ) as Record<string, unknown>;
+        let fetchCalled = false;
 
+        globalThis.fetch = async () => {
+          fetchCalled = true;
           return createJsonResponse(
             createMutationResponse()
           );
         };
 
         try {
-          await updateUsuarioZona(
-            createZona({
-              zona: '1',
-              nombre: '1 UNICA',
-              estado: false,
-            }),
-            true
+          await assert.rejects(
+            () =>
+              updateUsuarioZona(
+                createZona({
+                  idAsignacion: null,
+                  estado: false,
+                }),
+                true
+              ),
+            /nid_asignacion válido/i
           );
 
-          assert.equal(
-            capturedBody.nid_asignacion,
-            1
-          );
-          assert.equal(
-            capturedBody.zona,
-            '1'
-          );
-          assert.equal(
-            capturedBody.bestado,
-            true
-          );
+          assert.equal(fetchCalled, false);
         } finally {
           globalThis.fetch =
             originalFetch;
         }
       }
     ),
+
   ]
 );
diff --git a/src/features/gestion-usuarios/api/usuarioZonasApi.ts b/src/features/gestion-usuarios/api/usuarioZonasApi.ts
index 7b0ac4a..309fd56 100644
--- a/src/features/gestion-usuarios/api/usuarioZonasApi.ts
+++ b/src/features/gestion-usuarios/api/usuarioZonasApi.ts
@@ -232,21 +232,19 @@ export const fetchZonasAsignadasByClienteUsuario =
     }
   };
 
-const getAsignacionIdFromZona = (
-  zona: string
+const requireAsignacionId = (
+  zona: UsuarioZonaItem
 ): number => {
-  const normalizedZona =
-    assertZona(zona);
-  const idAsignacion = Number(
-    normalizedZona
-  );
+  const idAsignacion =
+    zona.idAsignacion;
 
   if (
+    idAsignacion === null ||
     !Number.isInteger(idAsignacion) ||
     idAsignacion <= 0
   ) {
     throw new Error(
-      `La zona "${normalizedZona}" no contiene un identificador de asignación válido.`
+      `La API no devolvió un nid_asignacion válido para la zona "${assertZona(zona.zona)}".`
     );
   }
 
@@ -267,25 +265,15 @@ const buildMutationBody = (
     'El identificador del cliente'
   );
 
-  const normalizedZona =
-    assertZona(zona.zona);
-
   return {
-    /*
-     * Contrato real del backend:
-     * - CreateAsignaUsuario crea una relación nueva con nid_asignacion = 0.
-     * - EditAsignaUsuario identifica la relación usando el código de zona
-     *   como nid_asignacion. No debe usarse el nid_asignacion del listado.
-     */
+    // POST crea una relación; PUT usa la PK real devuelta por la API.
     nid_asignacion:
       operation === 'create'
         ? 0
-        : getAsignacionIdFromZona(
-            normalizedZona
-          ),
+        : requireAsignacionId(zona),
     nid_usuario: zona.idUsuario,
     nid_cliente: zona.idCliente,
-    zona: normalizedZona,
+    zona: assertZona(zona.zona),
     bestado,
   };
 };
diff --git a/src/features/gestion-usuarios/mappers/usuarioZonas.mapper.test.ts b/src/features/gestion-usuarios/mappers/usuarioZonas.mapper.test.ts
index 499b935..298a603 100644
--- a/src/features/gestion-usuarios/mappers/usuarioZonas.mapper.test.ts
+++ b/src/features/gestion-usuarios/mappers/usuarioZonas.mapper.test.ts
@@ -20,6 +20,7 @@ const createZona = (
   zona: string,
   overrides: Partial<UsuarioZonaItem> = {}
 ): UsuarioZonaItem => ({
+  idAsignacion: null,
   idUsuario: 16068,
   idCliente: 59,
   zona,
@@ -41,12 +42,14 @@ export const suite = defineSuite(
               descripcionZona:
                 '550 Lima                ',
               bEstado: null,
+              nid_asignacion: null,
             },
             16068,
             59
           );
 
         assert.deepEqual(zona, {
+          idAsignacion: null,
           idUsuario: 16068,
           idCliente: 59,
           zona: '550',
@@ -66,11 +69,16 @@ export const suite = defineSuite(
               descripcionZona:
                 '650 Callao',
               bEstado: false,
+              nid_asignacion: 82450,
             },
             16068,
             59
           );
 
+        assert.equal(
+          zona.idAsignacion,
+          82450
+        );
         assert.equal(
           zona.estado,
           false
@@ -92,6 +100,7 @@ export const suite = defineSuite(
           });
 
         assert.deepEqual(zona, {
+          idAsignacion: 901,
           idUsuario: 16068,
           idCliente: 59,
           zona: '550',
@@ -144,6 +153,7 @@ export const suite = defineSuite(
         const zonas = dedupeUsuarioZonas([
           createZona('550'),
           createZona('550', {
+            idAsignacion: 901,
             estado: true,
             nombre: '550 Lima',
           }),
diff --git a/src/features/gestion-usuarios/mappers/usuarioZonas.mapper.ts b/src/features/gestion-usuarios/mappers/usuarioZonas.mapper.ts
index d3fef3e..62f1cd0 100644
--- a/src/features/gestion-usuarios/mappers/usuarioZonas.mapper.ts
+++ b/src/features/gestion-usuarios/mappers/usuarioZonas.mapper.ts
@@ -45,33 +45,47 @@ const buildZonaNombre = (
   return `${normalizedZona} ${normalizedDescription}`.trim();
 };
 
+const toPositiveIntegerOrNull = (
+  value: unknown
+): number | null => {
+  if (value === null || value === undefined) {
+    return null;
+  }
+
+  const parsed = Number(value);
+
+  return Number.isInteger(parsed) &&
+    parsed > 0
+    ? parsed
+    : null;
+};
+
 export const mapUsuarioZonaFaltante = (
   item: UsuarioZonaFaltanteApi,
   idUsuario: number,
   idCliente: number
-): UsuarioZonaItem => {
-  const estado =
-    typeof item.bEstado === 'boolean'
-      ? item.bEstado
-      : typeof item.bestado === 'boolean'
-        ? item.bestado
-        : null;
-
-  return {
-    idUsuario,
-    idCliente,
-    zona: normalizeZona(item.zona),
-    nombre: buildZonaNombre(
-      item.zona,
-      item.descripcionZona
+): UsuarioZonaItem => ({
+  idAsignacion:
+    toPositiveIntegerOrNull(
+      item.nid_asignacion
     ),
-    estado,
-  };
-};
+  idUsuario,
+  idCliente,
+  zona: normalizeZona(item.zona),
+  nombre: buildZonaNombre(
+    item.zona,
+    item.descripcionZona
+  ),
+  estado: item.bEstado,
+});
 
 export const mapUsuarioZonaAsignada = (
   item: UsuarioZonaAsignadaApi
 ): UsuarioZonaItem => ({
+  idAsignacion:
+    toPositiveIntegerOrNull(
+      item.nid_asignacion
+    ),
   idUsuario: Number(item.nid_usuario),
   idCliente: Number(item.nid_cliente),
   zona: normalizeZona(item.zona),
@@ -114,6 +128,8 @@ export const dedupeUsuarioZonas = (
 
     if (
       !current ||
+      (current.idAsignacion === null &&
+        zona.idAsignacion !== null) ||
       (current.estado !== true &&
         zona.estado === true)
     ) {
diff --git a/src/features/gestion-usuarios/modules/asignar-usuario/hooks/useAsignarUsuarioTable.ts b/src/features/gestion-usuarios/modules/asignar-usuario/hooks/useAsignarUsuarioTable.ts
index d76c9d3..f91d26b 100644
--- a/src/features/gestion-usuarios/modules/asignar-usuario/hooks/useAsignarUsuarioTable.ts
+++ b/src/features/gestion-usuarios/modules/asignar-usuario/hooks/useAsignarUsuarioTable.ts
@@ -183,12 +183,10 @@ export const useAsignarUsuarioTable = () => {
               ) ?? zona;
 
             /*
-             * bEstado=null: nunca existió la asignación -> POST.
-             * bEstado=false (o cualquier relación previa): reactivación -> PUT.
+             * bEstado=null + nid_asignacion=null: relación nueva -> POST.
+             * bEstado=false + nid_asignacion real: relación inactiva -> PUT.
              */
-            if (
-              persisted.estado !== null
-            ) {
+            if (persisted.estado === false) {
               if (!canEdit) {
                 throw new Error(
                   `No tiene permiso para reactivar la zona "${persisted.nombre}".`
diff --git a/src/features/gestion-usuarios/modules/asignar-usuario/types/usuarioZonas.types.ts b/src/features/gestion-usuarios/modules/asignar-usuario/types/usuarioZonas.types.ts
index 3be4452..c9dedd1 100644
--- a/src/features/gestion-usuarios/modules/asignar-usuario/types/usuarioZonas.types.ts
+++ b/src/features/gestion-usuarios/modules/asignar-usuario/types/usuarioZonas.types.ts
@@ -1,4 +1,5 @@
 export interface UsuarioZonaItem {
+  idAsignacion: number | null;
   idUsuario: number;
   idCliente: number;
   zona: string;
diff --git a/src/features/gestion-usuarios/types/usuarioZonas.types.ts b/src/features/gestion-usuarios/types/usuarioZonas.types.ts
index 0ac49d4..65c44f3 100644
--- a/src/features/gestion-usuarios/types/usuarioZonas.types.ts
+++ b/src/features/gestion-usuarios/types/usuarioZonas.types.ts
@@ -6,8 +6,8 @@ import type {
 export interface UsuarioZonaFaltanteApi {
   zona: string;
   descripcionZona: string;
-  bEstado?: boolean | null;
-  bestado?: boolean | null;
+  bEstado: boolean | null;
+  nid_asignacion: number | null;
 }
 
 export interface UsuarioZonaAsignadaApi {
