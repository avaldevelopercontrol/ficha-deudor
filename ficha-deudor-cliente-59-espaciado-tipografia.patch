--- a/src/features/ficha-deudor/modules/deudor-header/components/DeudorHeader.tsx
+++ b/src/features/ficha-deudor/modules/deudor-header/components/DeudorHeader.tsx
@@ -131,35 +131,47 @@
                 </>
               )}
 
-              <InfoRow
-                label={
-                  isClienteMaf
-                    ? 'Reprogramación Cuota Balón:'
-                    : 'Plazo Especial:'
-                }
-                value={deudorData.clienteConSinPe}
-                tone="danger"
-              />
-
-              <InfoRow
-                label={
-                  isClienteMaf
-                    ? 'Refinanciamiento Balón:'
-                    : 'White List:'
-                }
-                value={deudorData.clienteListaBlanca}
-                tone="danger"
-              />
-
-              <InfoRow
-                label={
-                  isClienteMaf
-                    ? 'Refinanciamiento Cuota Normal:'
-                    : 'Provision:'
-                }
-                value={deudorData.clientePorVision}
-                tone="danger"
-              />
+              {isClienteMaf ? (
+                <div className="deudor-header__maf-additional-fields">
+                  <InfoRow
+                    label="Reprogramación Cuota Balón:"
+                    value={deudorData.clienteConSinPe}
+                    tone="danger"
+                  />
+
+                  <InfoRow
+                    label="Refinanciamiento Balón:"
+                    value={deudorData.clienteListaBlanca}
+                    tone="danger"
+                  />
+
+                  <InfoRow
+                    label="Refinanciamiento Cuota Normal:"
+                    value={deudorData.clientePorVision}
+                    tone="danger"
+                  />
+                </div>
+              ) : (
+                <>
+                  <InfoRow
+                    label="Plazo Especial:"
+                    value={deudorData.clienteConSinPe}
+                    tone="danger"
+                  />
+
+                  <InfoRow
+                    label="White List:"
+                    value={deudorData.clienteListaBlanca}
+                    tone="danger"
+                  />
+
+                  <InfoRow
+                    label="Provision:"
+                    value={deudorData.clientePorVision}
+                    tone="danger"
+                  />
+                </>
+              )}
 
             </CompactInfoSection>
 
--- a/src/features/ficha-deudor/styles/08-compact-responsive-and-helpers.css
+++ b/src/features/ficha-deudor/styles/08-compact-responsive-and-helpers.css
@@ -362,6 +362,21 @@
   font-weight: 700;
 }
 
+/* Campos adicionales exclusivos del cliente MAF (59). */
+.deudor-header__maf-additional-fields {
+  display: flex;
+  flex-direction: column;
+  gap: 7px;
+}
+
+.ficha-sidebar .deudor-header__maf-additional-fields .compact-label {
+  font-size: 10px;
+}
+
+.ficha-sidebar .deudor-header__maf-additional-fields .compact-value {
+  font-size: 12px;
+}
+
 .compact-value--link {
   font-size: 10px;
   color: var(--ap-info);
--- a/src/features/ficha-deudor/modules/deudor-header/components/DeudorHeader.test.tsx
+++ b/src/features/ficha-deudor/modules/deudor-header/components/DeudorHeader.test.tsx
@@ -57,6 +57,7 @@
     );
 
     assert.match(html, /Información Adicional/);
+    assert.match(html, /deudor-header__maf-additional-fields/);
     assert.match(html, /Reprogramación Cuota Balón:/);
     assert.match(html, /Plazo especial de prueba/);
     assert.match(html, /Refinanciamiento Balón:/);
